Todos los errores de esta tabla son **normales**: le pasan a cualquiera que sigue el curso. El hábito correcto (Concepto 0b): lee la *última línea* del traceback y búscala aquí.

| El error que ves | Qué significa | Qué hacer |
| --- | --- | --- |
| `429 RESOURCE_EXHAUSTED` | Se agotó la cuota del plan gratuito de Gemini. **No es un bug de tu código.** | Espera unos minutos, cambia a `gemini-2.5-flash`, o deja que actúen tus `with_fallbacks`/`with_retry` (Concepto 09). En los [ejercicios](/recurso/ejercicios), la salida buena es `LLM_PROVIDER=groq` + `GROQ_API_KEY`: pasan al modelo por defecto de Groq sin tocar código. |
| `ModuleNotFoundError: No module named 'langchain…'` | La librería no está instalada en el entorno con el que corres. | `cd cursoIA/curso_ejemplos && uv sync`, y ejecuta siempre con `uv run python …` (no con el Python del sistema). El paso 3 de `uv run python env_utils.py` te dice cuáles faltan. |
| `Creating virtual environment at: .venv` seguido de `ModuleNotFoundError` | `uv` creó un entorno vacío porque el `pyproject.toml` que encontró no es el del curso: estás fuera de `curso_ejemplos/`. | `cd cursoIA/curso_ejemplos` y corre `uv sync`. Para confirmar dónde estás parado: `uv run python env_utils.py` (pasos 2 y 3). |
| El mismo comando funciona en una carpeta y falla en otra | `uv` elige el entorno según el `pyproject.toml` más cercano *hacia arriba*. Cambiar de carpeta cambia de `.venv`. | `uv run python -c "import sys; print(sys.prefix)"` te dice qué entorno usa de verdad. Haz `cd` al proyecto correcto. |
| "❌ Falta …_API_KEY" (`GOOGLE_API_KEY`, `GROQ_API_KEY`, `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `OPENROUTER_API_KEY`, `DEEPSEEK_API_KEY`…) | El `.env` no existe, está en otra carpeta, o pusiste la llave de un proveedor y `LLM_PROVIDER` apunta a otro. El mensaje siempre nombra **la variable del proveedor activo** — el único que `ollama` no necesita, porque corre en tu máquina. | Debe estar en `curso_ejemplos/.env`: `cp curso_ejemplos/.env.example curso_ejemplos/.env` y edítalo con la llave del proveedor que activaste. La lista completa y cómo elegir, en el [Concepto 18b](/concepto/18b-elegir-modelo-y-proveedor). |
| `404 · model_decommissioned` · `The model … has been deprecated` · `model not found` (con un ID que sale del curso) | **El proveedor apagó ese modelo.** No es tu código ni tu llave: los catálogos cambian varias veces al año. Le pasó a `qwen/qwen3-32b`, que fue el modelo de Groq del curso hasta 2026-07. | Dos pasos, sin tocar código: copia un ID vigente de la [tabla de modelos del cheat sheet](/recurso/cheat-sheet) —o de la web oficial del proveedor, que va por delante de nosotros— y ponlo en `LLM_MODELO=` de tu `.env`. Esa variable **gana** sobre cualquier default del curso. |
| `400 API key not valid` | La llave se copió mal (espacios, comillas, incompleta) o fue revocada. | Regenérala en **aistudio.google.com**. En el `.env` va sin comillas y sin espacios: `GOOGLE_API_KEY=AIza…` |
| `401 Unauthorized` · `402 Payment Required` · `insufficient credits/quota` | Estás en un proveedor **de pago** (OpenAI, Anthropic, DeepSeek, o un modelo no `:free` de OpenRouter) y la cuenta no tiene saldo, no verificó facturación, o la llave no autoriza ese modelo. No es un bug del código: es la tarjeta, no el `.env`. | Carga saldo en la consola del proveedor, o cámbiate a una opción gratuita: `LLM_PROVIDER=groq`, `LLM_PROVIDER=google`, un modelo `:free` de OpenRouter, u `ollama` en tu máquina. Qué proveedor cobra y cuánto, en el [Concepto 18b](/concepto/18b-elegir-modelo-y-proveedor). |
| `command not found: uv` | `uv` no está instalado (o la terminal no lo encuentra). | Instálalo con `curl -LsSf https://astral.sh/uv/install.sh \| sh` y abre una terminal nueva. |
| `IndentationError` / `SyntaxError` | Al copiar, el código perdió su alineación (Concepto 0b: la indentación es sintaxis). | Usa el botón **copiar** de cada bloque de este curso y pega en un archivo vacío, sin retocar espacios. |
| `AttributeError: … has no attribute …` | Escribiste un nombre que no existe (típico: `.contenido` en vez de `.content`) o tu versión de librería cambió esa pieza. | Revisa la ortografía exacta; luego `uv pip list \| grep langchain` — el curso usa `langchain 1.x` y `langgraph 1.x`. |
| Pusiste `LLM_PROVIDER=groq` y aun así te pide `GOOGLE_API_KEY` | Estás corriendo un ejemplo de **RAG** (11, 12 o el proyecto final). El chat va por Groq, pero los **embeddings** siguen en Gemini: Groq no los ofrece. | `uv sync --extra emb` y añade `EMBEDDINGS_PROVIDER=fastembed` al `.env`: los calcula en tu máquina, sin cuota. Ver el [Concepto 11](/concepto/11-rag-profesional). |
| Con Groq, las respuestas empiezan con `<think>…</think>` | Pusiste en `LLM_MODELO` un modelo de **razonamiento** de la familia Qwen o DeepSeek-R1: esos devuelven su cadena de pensamiento dentro del contenido. | Ya lo resuelve `util.crear_llm()` (`reasoning_format: hidden`). Si lo ves, es que instanciaste `ChatOpenAI` a mano. Para verlo a propósito: `GROQ_RAZONAMIENTO=raw`. Con el default del curso (`gpt-oss`) no pasa: su razonamiento va en un campo aparte. |
| `400 · reasoning_format is not supported with this model` | Cada familia esconde su razonamiento con **su** parámetro y no son intercambiables: `gpt-oss` usa `include_reasoning` (booleano), el resto de Groq usa `reasoning_format`. Son mutuamente excluyentes. | `util.crear_llm()` ya elige el correcto según el modelo. Si instancias `ChatOpenAI` a mano, mira qué acepta tu modelo en [console.groq.com/docs/reasoning](https://console.groq.com/docs/reasoning). |
| `400 · This model does not support response format json_schema` | `with_structured_output()` pide `json_schema`, y Groq solo lo soporta en algunos modelos — el del curso no está entre ellos. | Ya lo resuelve `util.crear_llm()` usando `method="function_calling"`. Si instancias el modelo a mano, pásale ese `method`. |
| `EMBEDDINGS_PROVIDER=fastembed` y falla con `No module named 'fastembed'` | El extra no está instalado. Va aparte porque descarga un modelo de ~220 MB. | `uv sync --extra emb`. Cambiar de modelo de embeddings **invalida cualquier índice ya construido**: hay que reindexar (en el curso da igual, se reconstruye en cada arranque). |
| `onnxruntime … doesn't have a … wheel for the current platform` (en `uv add` de `chroma`/`fastembed`) | Estás en un **Mac Intel** (`x86_64`). `chromadb` arrastra `onnxruntime`, que desde la **1.24** ya no publica binarios para esa plataforma. | En tu `pyproject.toml` añade `[tool.uv]` con `constraint-dependencies = ["onnxruntime<1.24"]` y repite el `uv add`: resuelve a la **1.23.2** (la última con wheel Intel). Solo afecta a Macs Intel. |
| El script "se queda pensando" sin imprimir | Está esperando a la API (normal en la primera llamada) o el stream no vacía el buffer. | Dale 15–30 segundos. En streaming, imprime con `flush=True` como hacen los ejemplos. |

<Box kind="analogy" label="◆ Si nada de esto aplica">

Copia la última línea del traceback completa y pégasela a una IA junto con el bloque de código que corriste. Describir el error con precisión *es* una habilidad de este curso.

</Box>
