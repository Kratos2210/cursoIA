export const meta = {
  "id": "m32",
  "slug": "32-frameworks-llamaindex-crewai",
  "title": "Correr otro framework: LlamaIndex y CrewAI en la práctica (anexo opcional)",
  "kind": "module",
  "level": 4,
  "levelName": "RAG",
  "order": 40,
  "badge": "32",
  "pyFile": "frameworks/rag_llamaindex.py",
  "fileref": "📋 Anexo. Vuelve ejecutables los snippets ilustrativos del m26: frameworks/rag_llamaindex.py + crew_crewai.py — fuera del gate offline (ADR-0006): extras opcionales, necesitan cuota de LLM, no se testean.",
  "tested": false,
  "goals": "🎯 Al terminar sabrás: ejecutar el mismo RAG del curso en LlamaIndex y una colaboración por roles en CrewAI, y sentir en vivo el tradeoff 'más alto nivel = menos control'.",
  "prereqs": "✅ Necesitas: el m11/m12 (RAG), el m15 (supervisor) y el m26 (mapa del ecosistema). Para ejecutarlo: una API de LLM con cuota. Es lectura si no la tienes.",
  "minutes": "⏱️ ~20 min de lectura (+ ejecución si tienes cuota)"
};

El [m26](/modulo/26-mapa-del-ecosistema) te enseñó el mapa de frameworks y te mostró el **"sabor"** de otras APIs con snippets **que no se ejecutaban**. Este anexo los vuelve **ejecutables**: correr LlamaIndex y CrewAI de verdad fija mucho mejor la lección que solo leerlos. Y la lección no es "cuál es mejor", es **reconocer la misma idea con otra piel**.

<Box kind="warn" label="▲ Anexo opcional, fuera del 'offline-first'">

LlamaIndex y CrewAI son **ecosistemas propios** (no LangChain): muchas dependencias y una API de LLM con cuota. Por eso viven en `frameworks/` con extras opcionales (`uv sync --extra llamaindex` / `--extra crewai`) y **fuera** del gate offline — la CI no los instala ni los testea (`docs/adr/0006-frameworks-alternativos-fuera-del-gate.md`). Ambos apuntan al **mismo endpoint** del `.env` que ya usas. Es lectura si no tienes cuota.

</Box>

### LlamaIndex: el mismo RAG, con menos andamiaje

Lo que en el [m11](/modulo/11-rag-profesional) fueron varias piezas (loader → splitter → embeddings → vector store → recuperar → prompt) LlamaIndex lo resuelve en tres líneas. **"Los datos primero"**:

```python title="frameworks/rag_llamaindex.py · el núcleo"
documentos = SimpleDirectoryReader(input_files=[str(CORPUS)]).load_data()
indice = VectorStoreIndex.from_documents(documentos)   # trocea + indexa por ti
motor = indice.as_query_engine(similarity_top_k=3)     # recuperar + prompt + LLM
respuesta = motor.query("¿Cuál es el horario de atención?")
```

<Box kind="def" label="◈ ¿Es el mismo RAG del m11? Sí">

`as_query_engine` hace **recuperar + montar prompt + llamar al LLM** — exactamente el pipeline del m11. Lo que cambia es cuánto **ves y tocas**: aquí `from_documents` elige el troceado y el índice por ti. Menos código, menos control. El curso usa los **mismos embeddings fastembed locales** (sin cuota) para que el RAG no dependa de que tu proveedor de chat ofrezca embeddings.

</Box>

### CrewAI: describes roles, la "crew" coordina

El [m15](/modulo/15-multiagente-supervisor) armó el patrón supervisor con un `StateGraph` a mano. CrewAI sube el nivel: das **roles y tareas** y él encadena. Sin grafo, sin estado, sin aristas a la vista:

```python title="frameworks/crew_crewai.py · roles en vez de grafo"
investigador = Agent(role="Investigador", goal="Reunir 3 datos fiables", llm=llm, ...)
redactor     = Agent(role="Redactor", goal="Convertirlos en un párrafo", llm=llm, ...)
redactar = Task(description="...", agent=redactor, context=[investigar])  # recibe lo del investigador
Crew(agents=[...], tasks=[...], process=Process.sequential).kickoff()
```

<Box kind="analogy" label="◆ Furgoneta manual vs bus con conductor (del m26)">

LangGraph es la *furgoneta con caja manual*: ves cada marcha (nodo, arista, estado) y la metes tú. CrewAI es el *bus con conductor*: subes, dices el destino en roles y él coordina. Cómodo para prototipar rápido una colaboración legible; incómodo el día que necesites **auditar cada transición** — el alto nivel te esconde el volante.

</Box>

<Avoid label="⛔ La trampa de correr otro framework y sacar la conclusión equivocada">

- **Creer que "menos código" es "mejor".** Menos código es menos control. Para una demo, LlamaIndex/CrewAI vuelan; para producción auditable, el m15 en LangGraph te deja ver y testear cada paso.
- **Olvidar que la disciplina es tuya.** DI, `pytest -m offline` y observabilidad no vienen del framework (m26): los montas tú alrededor de casi cualquiera. No los pierdas al cambiar de piel.
- **Migrar por hype.** Cambiar de framework no es gratis: reescribes pegamento, tests y tracing. Corre estos para *entenderlos*, no para saltar sin motivo.

</Avoid>

<Checkpoint label="✔ Si llegaste aquí deberías poder…">

- Mapear `VectorStoreIndex.from_documents` + `as_query_engine` al pipeline RAG del m11.
- Explicar qué esconde CrewAI respecto al `StateGraph` del m15 (grafo, estado, transiciones).
- Enunciar el tradeoff del m26 con un ejemplo que corriste, no de memoria.
- Justificar por qué DI/tests/observabilidad siguen siendo tuyos, no del framework.

</Checkpoint>

<Recap level={4} label="↺ En una frase">

Correr LlamaIndex (el RAG del m11 en tres líneas) y CrewAI (el supervisor del m15 en roles) te deja **sentir** el tradeoff del m26: cada framework decide la ergonomía del flujo, pero la disciplina de ingeniería —y la elección de cuándo abrir el capó— la pones tú. Anexo opcional, fuera del gate.<Arch>artefactos: `frameworks/rag_llamaindex.py` · `frameworks/crew_crewai.py` · extras `llamaindex`/`crewai` · ADR-0006</Arch>

</Recap>
