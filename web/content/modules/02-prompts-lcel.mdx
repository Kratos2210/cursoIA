<LevelBanner level={2} title="Empieza la Ruta 2 · Prompts y composición">

Del mensaje suelto a la línea de ensamblaje: prompts con oficio, cadenas LCEL, salida con forma de dato, memoria y apps que no se caen — y un proyecto que lo junta todo.

</LevelBanner>

Un **prompt template** es un molde con huecos `{variables}`. Y aquí aparece la idea central de LangChain moderno: **LCEL**, que conecta pasos con el operador tubería `|`.

<Box kind="def" label="◈ Definición · LCEL">

**LCEL** (*LangChain Expression Language*, Lenguaje de Expresión de LangChain) es una forma **declarativa** de construir cadenas de procesamiento: en lugar de escribir paso a paso *cómo* se llama a cada pieza, **declaras el orden** en que se conectan y LangChain se encarga del resto.

"Declarativo" tiene una consecuencia práctica y no es decoración: como LangChain conoce la forma completa de la cadena antes de ejecutarla, puede darte gratis paralelismo en `batch`, streaming token a token, versiones asíncronas y reintentos — sin que tú escribas nada de eso.

#### La magia del operador tubería (`|`)

La sintaxis imita las **tuberías de Unix**: `|` encadena componentes de modo que **la salida de uno es la entrada del siguiente**. Una cadena básica tiene tres bloques:

Prompt (plantilla)  |  Modelo (LLM)  |  Parser (analizador de salida)

<DefList>
  <Term>PromptTemplate</Term>
  <Def>El **molde** de tus instrucciones. En vez de un mensaje fijo, defines una plantilla con variables (`"Traduce al {idioma}: {texto}"`) y reutilizas la misma estructura para entradas distintas.</Def>
  <Term>Model</Term>
  <Def>El **cerebro**: el modelo de lenguaje (Gemini, GPT, Claude). Recibe el prompt ya rellenado y genera la respuesta.</Def>
  <Term>OutputParser</Term>
  <Def>El **formateador**. Toma la respuesta cruda del modelo (un `AIMessage`) y la convierte en algo que tu programa pueda usar directo: texto plano con `StrOutputParser`, o un objeto/JSON con parsers estructurados.</Def>
</DefList>

</Box>

<Box kind="analogy" label="◆ Analogía">

El `|` es una **línea de ensamblaje**: el dato entra por la izquierda y avanza de estación en estación. *prompt* arma el mensaje → *modelo* lo responde → *parser* limpia la salida.

</Box>

```python title="chain.py · tu primera cadena LCEL"
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser

prompt = ChatPromptTemplate.from_template(
    "Eres analista de atención al cliente. Clasifica el sentimiento "
    "(Positivo/Negativo/Neutro) de esta reseña y responde en una frase.\n"
    "Reseña: {review}"
)

cadena = prompt | llm | StrOutputParser()   # prompt -> modelo -> texto limpio

print(cadena.invoke({"review": "El producto llegó tarde y roto."}))
```

```text title="▸ deberías ver (algo así)"
Negativo. El cliente expresa molestia porque el producto llegó tarde y dañado.
```

`StrOutputParser()` convierte la respuesta del modelo en **texto plano**, para no escribir `.content` todo el tiempo.

<Box kind="def" label="◈ En qué situaciones se usa">

- **El mismo análisis sobre entradas que rotan.** Clasificar cada reseña que entra, resumir cada ticket de soporte: el molde de instrucciones es fijo y el dato cambia a cada rato — tú declaras la cadena una vez y la entrada es lo único variable.
- **Un pipeline de 2+ pasos que mañana querrás correr por lotes o en streaming.** Al declararlo como cadena, `batch` y `stream` te salen gratis sin reescribir nada ([c03](/concepto/03-interfaz-unificada)).
- **Piezas que quieres intercambiar sin tocar el resto.** Cambiar el prompt o el modelo sin reescribir la tubería: la topología es una cosa y las instrucciones, otra.
- **Y el contrapunto:** para una exploración de una sola vez en un notebook, un `llm.invoke()` suelto basta — la cadena paga cuando el flujo se repite.

</Box>

<Box kind="practice" label="▸ Para practicar">

Modifica el prompt para que además **traduzca la reseña al inglés**. Solo cambias el molde: la cadena `prompt | llm | parser` sigue igual. Eso es el poder de LCEL.

<Solution summary="Ver una solución">

```python title="solución · solo cambia el molde"
prompt = ChatPromptTemplate.from_template(
    "Eres analista de atención al cliente. Para esta reseña:\n"
    "1) Clasifica el sentimiento (Positivo/Negativo/Neutro).\n"
    "2) Tradúcela al inglés.\n"
    "Reseña: {review}"
)
# la cadena NO se toca:
print((prompt | llm | StrOutputParser()).invoke({"review": "El producto llegó tarde y roto."}))
```

</Solution>

</Box>

<Checkpoint label="✔ Si llegaste aquí deberías poder…">

- Explicar qué es un prompt template y por qué un molde con `{variables}` es reutilizable.
- Decir qué hace el operador `|`: la salida de cada pieza es la entrada de la siguiente.
- Nombrar los tres bloques de una cadena básica (prompt `|` modelo `|` parser).
- Reconocer para qué sirve `StrOutputParser()` (dejar texto plano en vez de un `AIMessage`).

</Checkpoint>

<Quiz title="✅ Autoevaluación · Concepto 02">

<Question>
  <Prompt>1 · ¿Qué ganas con un `ChatPromptTemplate` frente a armar el texto con una f-string cada vez?</Prompt>
  <Option>El modelo procesa la plantilla más rápido</Option>
  <Option ok>Un molde reutilizable: cambias la entrada sin reescribir las instrucciones</Option>
  <Option>Las variables `{}` se validan contra un esquema Pydantic</Option>
  <Feedback>La plantilla separa *las instrucciones* (fijas, en un solo sitio) de *los datos* (variables). Validar con Pydantic es otra cosa: eso llega en el Concepto 05.</Feedback>
</Question>

<Question>
  <Prompt>2 · LCEL es "declarativo". ¿Qué te regala eso en la práctica?</Prompt>
  <Option>Que el modelo entiende mejor el prompt porque va estructurado</Option>
  <Option ok>Que LangChain conoce la cadena entera antes de correrla, y de ahí salen gratis el paralelismo, el streaming y las versiones async</Option>
  <Option>Que la cadena se guarda en disco y no hay que reconstruirla</Option>
  <Feedback>Declaras el *orden*, no el *cómo*. Al conocer la forma completa, LangChain puede optimizarla — por eso el Concepto 03 te da `batch` y `stream` sin escribir nada.</Feedback>
</Question>

<Question>
  <Prompt>3 · Quieres que además de clasificar el sentimiento, la cadena traduzca la reseña al inglés. ¿Qué tocas?</Prompt>
  <Option>Añades un segundo `llm` a la cadena con otro `|`</Option>
  <Option ok>Solo el molde del prompt: `prompt | llm | parser` no cambia</Option>
  <Option>Cambias el parser por uno que soporte dos salidas</Option>
  <Feedback>Ese es todo el argumento de LCEL: la topología es una cosa y las instrucciones son otra. Casi siempre lo que cambia es el molde (Concepto 02).</Feedback>
</Question>

</Quiz>

<Recap level={2} label="↺ En una frase">

Un prompt es una **plantilla reutilizable**, no un texto fijo. Y el operador `|` encadena prompt → modelo → parser en un solo objeto invocable.<Arch>archivo: `02_prompts_lcel.py`</Arch>

</Recap>
