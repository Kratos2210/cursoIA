"""El servicio: configuración, modelos, RAG, agente y API.

⚠️ Este archivo no está vacío por capricho. Sin él, `app/` sería un
   "namespace package" y `import app` resolvería al módulo `proyecto_final/app.py`
   (el shim de compatibilidad), porque un módulo regular SIEMPRE gana a un
   namespace package, esté antes o después en el sys.path.
"""
